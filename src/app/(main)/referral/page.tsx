import { Referral } from "@/screens/referral";
import { getMetaData } from "app/(main)/layout";

export const metadata = getMetaData({
  name: "Referral",
  description: "Referral page platformz",
});

export default Referral;
