export const renderEmail = (email) => {
  return <span className="text-base text-[#74788D]">{email}</span>;
};

export const renderPhoneNumber = (phoneNumber) => {
  return <span className="text-[#74788D]">{phoneNumber}</span>;
};
